# SPA fallback：未知路径回退到 index.html（由前端 hash 路由接管）
# 注：Cloudflare 构建日志的 "Infinite loop ... ignored" 警告源于 auto pretty-URL
# 剥离（/index.html → /index 再被本规则指回）。不影响功能（/* 规则仍有效）。
# 永久消除：Dashboard → Pages → ulink → Settings → General → HTML Handling → None
# （本项目无 .html 静态链接需要剥离，关闭零副作用）。
/*    /index.html   200
